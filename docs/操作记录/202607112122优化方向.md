# 当前问题与优化方向

- **记录时间：** 2026-07-11 21:22（北京时间）
- **状态：** 待优化清单

## Top 5 问题

1. **历史新闻不足** — 本地缓存仅数天；三年回测用 96 条 NFP/CPI/FOMC 日程代理，非真实新闻文本
2. **小账户易强平** — $500 + 100x + 间距 2.0 + 5 层 → 回测一跑就 stop_out（见 `202607112109`）
3. **五因子未校准** — 权重/阈值手工设定，低相关新闻易给 0 分或假方向
4. **模拟器简化** — 无滑点/点差，结果仅作 pipeline 验证，不宜写年化收益结论
5. **News Adapter 未显优势** — 冲击组更早强平、回撤更大，需配合减层/停单规则

## 下一步（3 项）

| 序 | 动作 | 命令/位置 |
|----|------|-----------|
| 1 | **$500 网格调参**（间距 3.5，max_layers 3）✅ `202607112128` | `config.yaml` → `backtest` 段 |
| 2 | 扩充宏观信号 96→439（日历+扩展日程）✅ `202607112147` | `build_historical_signals.py --years 3` |
| 3 | 三年回测同步 `docs/汇报` ✅ | Run ID `202607112147` |

```powershell
python scripts/run_news_grid_backtest.py --years 3 --initial 500
python scripts/generate_trade_log_report.py --run-id {新run_id}
```

## 索引

- [00_回测索引.md](../汇报/00_回测索引.md) · [07_回测命名规范.md](07_回测命名规范.md)

---

## 2026-07-11 21:57 价格上下文 + K线评估

**已完成：**

1. `historical_signals.py` — 事件时点融合 H1 动量（`momentum_at_time`），`price_blend=0.20`；动量与新闻方向冲突时降级中性并降分
2. `config.yaml` / `config.example.yaml` — 新增 `trend_scoring.price_blend: 0.20`、`direction_threshold: 12`
3. 新增 `scripts/eval_kline_forecast.py` — H1 走查评估未来 4 根方向命中率

**信号重建：** 439 条（与 `202607112147` 持平）

**三年回测 Run ID `202607112157`（$500，间距 3.5，3 层）：**

| 组别 | PnL | 对比 `202607112147` |
|------|-----|---------------------|
| baseline | **-480.04** | 持平 |
| news_adapter | **-459.79** | 持平（+20.25 vs baseline） |

价格上下文融合未改变回测 PnL（冲击动作仍为 74 次），说明 composite_score 微调未触发不同停单/间距分支。

**K 线预测评估（1 年走查，仅动量）：** 命中率 **31.36%**，评分 MAE 72.56，评估步数 5578 → `202607112157_K线预测评估.md`

---

## 2026-07-11 22:21 价格动量冲击策略

**根因：** 宏观日程信号 `direction=neutral`（词典无情感），`composite_score` 微调不改变 `pause_reverse`/`pause_all_new`；L3 冲击动作来自 `pause_all_new=True`。

**改动：**

1. `grid_adapter.py` — 新增 `PriceShockConfig`、`apply_price_shock_adjustment()`；中性新闻 + |动量|>25 时降级冲击（放宽间距、取消停单）；有方向时按动量冲突/一致调整
2. `historical_signals.py` — 写入 `price_momentum`、`_price_shock_note`，信号构建时应用价格冲击调整
3. `policy.py` — `ShockPolicy` 增加 `price_momentum` 字段
4. `trend_scorer.py` — factors 输出 `price_momentum`
5. `config.yaml` — `grid_strategy.price_shock_enabled`、`momentum_conflict_threshold: 25`、`momentum_align_boost: 0.1`

**信号变化：** 222/439 条降级冲击；`pause_all_new` 149→30

**三年回测 Run ID `202607112221`（$500，间距 3.5，3 层）：**

| 组别 | PnL | 冲击动作 | 对比 `202607112157` |
|------|-----|----------|---------------------|
| baseline | **-480.04** | 0 | 持平 |
| news_adapter | **-466.89** | **57** | PnL 变差 7.1，冲击 -17 |

news_adapter 仍优于 baseline（+13.15），但较上轮 +20.25 收窄；强动量时取消 L3 停单减少了防御性冲击，结果变差说明部分停单在该窗口有保护作用。

### 调参跟进（202607112222）

- `momentum_conflict_threshold`: 25 → **35**
- 中性新闻 + 强动量：**仅放宽间距**，保留 `pause_all_new`（`neutral_widen_only: true`）

| 组别 | PnL | 冲击动作 |
|------|-----|----------|
| baseline | -480.04 | 0 |
| news_adapter | **-460.10** | **74** |

较 `202607112221` 恢复至接近最优（`202607112157` 为 -459.79）；说明 L3 停单保护应保留，动量只用于微调间距。

### 宏观事件方向规则（202607120109）

**改动：** `historical_signals.py` 新增方向解析链：词典 → 实际/预期意外 → H1 动量（|m|≥12）

**信号方向分布（439 条）：** bullish 188 / bearish 201 / neutral 50（原几乎全 neutral）

| 组别 | PnL | vs baseline | 冲击动作 |
|------|-----|-------------|----------|
| baseline | -480.04 | — | 0 |
| news_adapter | **-452.43** | **+27.61** | **102** |

较 `202607112222`（-460.10，+19.94）进一步改善；L2/L3 的 `pause_reverse` / 反向减仓开始生效。

### 宏观 actual/forecast + 参数扫描（202607120114）

**actual 来源：** `macro_actuals.py` — 日历 parquet 合并 + FRED 缓存（nfp/claims/gdp 4469 行）+ 可选 `macro/macro_releases.csv`

**方向来源：** surprise **217** / momentum 194 / neutral 125

| 组别 | PnL | vs baseline | 冲击 |
|------|-----|-------------|------|
| baseline | -480.04 | — | 0 |
| news_adapter | **-451.61** | **+28.43** | 102 |

**参数扫描 `202607120112`（spacing 3.5–5.0 × layers 2–3）：** 8 组均 stop_out；最优仍为 spacing=3.5、layers=3（news +27.61）。放宽间距至 5.0 可略降 DD（93.5%）但 vs baseline 收窄。

**脚本：** `fetch_macro_actuals.py`、`scan_grid_params.py`

### 三项 stop_out 优化（202607120120）

1. **过滤 claims** — 剔除 159 条每周初请失业金，信号 439→**280**
2. **L3 冲击窗口 4h** — `shock_duration_hours`（原 24–48h）
3. **冲击窗口内单向网格** — 有方向 + L2/L3 时只挂顺势侧

| 组别 | PnL | vs baseline | stop_out |
|------|-----|-------------|----------|
| baseline | -480.04 | — | Y |
| news_adapter | **-451.61** | **+28.43** | Y |

PnL 与 `202607120114` 持平，信号更精简；仍强平，下一步接 margin 预警 overlay。

### Margin 预警 overlay（202607120126）

**实现：** `risk_overlay.py` — 对齐 `risk_control`  
- margin_level ≤ **80%**：禁止新挂单，止盈间距 ×1.2  
- margin_level ≤ **65%**：撤销全部挂单  
- 峰值回撤 ≥ **14%**（20%×0.7）：同上预警  

| 组别 | PnL | vs baseline | max DD | stop_out | risk_actions |
|------|-----|-------------|--------|----------|--------------|
| baseline | -480.04 | — | 97.8% | **Y** | 3 |
| news_adapter | **-133.09** | **+346.95** | **43.8%** | **N** | 0 |

**首次避免强平**；news_adapter 跑完全程 280 信号，冲击动作 278（此前因早停仅 ~90）。

### Live 同步 + margin 敏感性（202607120130）

- `RiskStateMachine` 对齐回测 overlay（margin_level ≤80 禁新单，≤65 撤全部挂单）
- `GridExecutor`：`cancel_all_pending`、`spacing_multiplier` 已接入
- `signal_bridge` 传递 `peak_equity`

**敏感性（warning_margin_level 75/80/85/90）：** news_adapter 结果一致（PnL -133.09，无 stop_out），说明本轮 news 路径未触发 overlay 预警；保护主要来自新闻冲击减仓 + 较短 L3 窗口。

### FRED 补全 + 回测（202607121039）

- FRED 缓存 **6640 行**（含 cpi/pce/retail）
- 方向来源：surprise **166** / momentum 99 / neutral 89

| 组别 | PnL | vs baseline | stop_out |
|------|-----|-------------|----------|
| baseline | -480.04 | — | Y |
| news_adapter | **-147.17** | **+332.87** | **N** |

仍无强平；较 `202607120126`（-133）略差，因 surprise 方向更分散。

### MT5 dry-run（20260712）

- 已连接 FxPro Demo **591852231**，`dry_run_risk_overlay` 三档通过
- `verify_l2_execution --dry-run` 计划动作正常；需 `--setup` 挂单后验删单

---

## 2026-07-12 10:58 Surprise 方向调优 + 回测

**代码改动：**

1. `macro_actuals.py` — `FRED_VALUE_MODE`（cpi/pce/retail/gdp 用 pct，nfp/claims 用 diff）；`_fred_release_frame()` 生成 actual/forecast/previous；`apply_fred_cache` 月度 month-match、claims 7d、其他 21d
2. `historical_signals.py` — 品类 `_surprise_is_significant` 阈值；surprise 方向在动量冲突时保留方向、仅调分；移除 `filter_events` 死代码
3. 新增 `scripts/eval_surprise_direction.py` — surprise 信号 vs 事件后 4 根 H1 方向命中率

**FRED 缓存重建：** **6634 行**（claims 3103 / nfp 1048 / cpi 950 / pce 807 / retail 411 / gdp 315）

**信号（280 条，剔除 claims）：**

| 方向来源 | 数量 |
|----------|------|
| surprise | **160** |
| momentum | 103 |
| none | 17 |
| dict | 0 |

**Surprise 方向命中率（4h / 4 根 H1）：** **49.35%**（76/154，6 条实际涨跌不足 0.5 USD 跳过）；CPI 最高 56.8%，PCE 最低 44.1%

**三年回测 Run ID `202607121058`（$500，间距 3.5，3 层）：**

| 组别 | PnL | vs baseline | max DD | stop_out |
|------|-----|-------------|--------|----------|
| baseline | -480.04 | — | 97.8% | Y |
| news_adapter | **-70.96** | **+409.08** | **36.6%** | **N** |

**对比历史最优：**

| Run ID | news PnL | stop_out | surprise 数 |
|--------|----------|----------|---------------|
| `202607120126` | -133.09 | N | 217→（旧阈值） |
| `202607121039` | -147.17 | N | 166 |
| **`202607121058`** | **-70.96** | **N** | **160** |

本轮较 `202607120126` 改善 **62.13**；品类阈值 + FRED pct/diff 模式使 surprise 更精准，冲击动作 241 次、无强平。

**gs_v17 集成：** `patch_gs_v17.py` 已提供 `patch_backtest_v1`（注入 `timeline_policy_fn`）与 `load_timeline_from_parquet`；完整 bar-loop hook 需 golden_shield_v1.7 源码，当前 standalone A/B 用 `run_news_grid_backtest.py` 即可。

---

## 2026-07-12 gs_v17 深度集成（M1 全引擎）

**改动：**

1. `news_shock_adapter/shock_bridge.py` — `apply_shock_on_activate`（撤反向挂单、按比例平反向仓）、`news_shock_overrides`（spacing/pause/directional）
2. `gs_v17/engine.py` — `run(timeline_policy_fn=...)`；bar 内在 `grid_step` 计算后注入冲击策略；L2+ 方向性网格仅挂顺势侧；stats 增加 `news_shock_actions`
3. `patch_gs_v17.py` — `run_news_shock_backtest()` 辅助函数
4. `scripts/run_gs_v17_news_backtest.py` — baseline vs news_adapter A/B，输出至 `05 Backtest/results/news-grid/`

**前置条件：**

- `GS_STRATEGY_ROOT` 或默认 `E:/量化项目/golden_shield_v1.7` 策略源码
- M1/M5 Parquet 在 `02 History Data/clean/`
- `historical_signals.parquet` 已构建

**命令：**

```powershell
# 发烟（约 1 周 M1）
python scripts/run_gs_v17_news_backtest.py --smoke

# 三年正式回测
python scripts/run_gs_v17_news_backtest.py --years 3
```

**说明：** 简化 H1 模拟器（`run_news_grid_backtest.py`）仍用于快速 pipeline 验证；gs_v17 集成为 M1 全引擎 A/B，含点差/费用/DD gate，结果可与生产回测对齐比较。

---

## 2026-07-12 12:48 执行顺序优化批次（PCE + 动量 + 参数扫描）

**配置改动：**

- PCE：L3→L2，`surprise_direction_exclude: [pce]`（33 条 `surprise_neutral` 仅调分不给方向）
- 动量：`momentum_direction_threshold: 18`（保留方向链，过滤弱动量）
- 网格：`base_spacing 5.0`、`max_layers 2`（扫描最优）

**信号（280 条）：** surprise 127 / momentum 100 / surprise_neutral 33 / none 20

**Surprise 命中率：** **50.0%**（122 条评估；PCE 方向已排除后仅 2 条 surprise 样本）

**三年回测：**

| Run ID | 参数 | news PnL | vs baseline | stop_out |
|--------|------|----------|-------------|----------|
| `202607121058`（上轮最优） | 3.5×3 | -70.96 | +409 | N |
| `202607121249` | **5.0×2** | **-51.25** | **+435** | N |
| `202607121249_02` | 3.5×3 + 新信号 | -57.99 | +422 | N |

**参数扫描 `202607121248`：** 8 组均 news 无 stop_out；最优 spacing=5.0 layers=2（news -206.64 为动量关闭版；动量@18 后 -51.25）

**阻塞：** gs_v17 仍缺 `golden_shield_v1.7`；L2 `--setup` 待周一开盘。

**macro 数据：** 已放置 `02 History Data/macro/macro_releases.csv.example` 供手工补 forecast。

**golden_shield_v1.7：** 不纳入毕设必选项；自研 `GridShockSimulator` 为主回测引擎（见 `docs/毕设/`）。

---

## 2026-07-12 14:44 自研引擎：点差 + M1

- `simulator.py` — `spread_price`（默认 0.20 USD 全价差），买卖开平按 bid/ask  
- `load_m1_parquet()` — M1 回测；正式窗口默认 30 天（`m1_max_days`）  
- `run_news_grid_backtest.py --timeframe M1`  
- 毕设文档骨架：`docs/毕设/`（系统定位、引擎说明、论文章节清单）  
- M1 smoke Run `202607121344`：14036 bars / 14d，引擎跑通


